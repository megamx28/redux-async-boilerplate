language: node_js

node_js:
  - stable

cache:
  directories:
    - node_modules

before_script:
  - npm install

script:
  - npm run lint
  - npm run test:coverage

after_success:
  - CODECLIMATE_REPO_TOKEN=CODECLIMATE_REPO_TOKEN codeclimate-test-reporter < coverage/report-lcovonly.txt
